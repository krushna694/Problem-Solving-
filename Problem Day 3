Given an integer array nums, determine whether any element appears more than once. Return true if a duplicate exists, and false otherwise.
Example 1:
Input: nums = [1, 1, 2, 3]
Output: true
Example 2:
Input: nums = [1, 2, 3, 4]
Output: false

class Solution {
    public boolean hasDuplicate(int[] nums) {
        Set<Integer> hasSeen = new HashSet<Integer>();

        for (int num:nums){
            if (hasSeen.contains(num)){
                return true;
            }
            hasSeen.add(num);
        }   
        return false;
    }
}
